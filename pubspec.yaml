name: eye_care_ai
description: "EyeCare AI - Theo dõi thói quen bảo vệ mắt."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.2.1+alpha

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  path_provider: ^2.1.5
  google_fonts: ^6.2.1
  fl_chart: ^0.70.2
  provider: ^6.1.2

  flutter_local_notifications: ^19.4.0
  timezone: ^0.10.1
  dio: ^5.9.0
  package_info_plus: ^9.0.0
  # Mở file APK vừa tải về bằng trình cài đặt gói của hệ thống (dùng cho
  # tính năng "Cập nhật app từ GitHub Releases", xem lib/services/update_service.dart) —
  # package này tự lo việc tạo content:// URI qua FileProvider riêng của nó,
  # không cần khai báo FileProvider thủ công trong AndroidManifest.
  open_filex: ^4.5.0

  # --- Real device data sources for the Habits screen ---
  sensors_plus: ^6.0.1     # accelerometer (reading posture, sleep motion heuristic)
  camera: ^0.11.0+2         # camera trước (selfie) để đo khoảng cách mắt-màn hình lúc kiểm tra thị lực
  google_mlkit_face_detection: ^0.13.1 # nhận diện vị trí 2 mắt trên khung hình camera
  geolocator: ^13.0.1      # GPS - nguồn dữ liệu duy nhất cho Outdoor time
  light: ^5.0.0             # cảm biến ánh sáng (lux) - dùng cho cảnh báo dùng điện thoại trong bóng tối + tự động điều chỉnh độ sáng
  screen_brightness: ^2.1.1 # chỉnh độ sáng màn hình theo lux đọc được từ cảm biến ánh sáng
  app_usage: ^4.1.0        # OS screen-on time (Android only, needs Usage Access permission)
  # health package đã BỊ BỎ — Sleep giờ ước lượng từ Usage Events (xem
  # UsageStatsHandler.getSleepEstimate()), không cần Health Connect nữa.
  permission_handler: ^11.3.1
  shared_preferences: ^2.3.3 # persists today's accumulated minutes across app restarts
  android_intent_plus: ^5.2.0 # mở thẳng màn hình cấp quyền Usage Access

  # --- Đăng nhập / tài khoản ---
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0
  google_sign_in: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

# Cấu hình đổi icon app — chạy lệnh:
#   dart run flutter_launcher_icons
# sau khi `flutter pub get`, icon sẽ tự sinh cho cả Android (bao gồm
# adaptive icon Android 8+) và iOS từ 2 file trong assets/icon/. Xem hướng
# dẫn chi tiết hơn (đổi ảnh khác, đổi màu nền...) ở phần chat.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  # Android 8+ (adaptive icon): lớp nền TRONG SUỐT + lớp hình mắt AI được co
  # nhỏ vào "vùng an toàn" ở giữa (app_icon_foreground.png) để không bị cắt
  # mất khi hệ thống bo tròn/bo vuông/giọt nước icon tuỳ launcher của máy.
  # "#00FFFFFF" = trắng với alpha 00 (trong suốt hoàn toàn), không phải "#FFFFFF"
  # (trắng đục) — đổi thành FFFFFF nếu sau này muốn quay lại nền trắng.
  adaptive_icon_background: "#00FFFFFF"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  # iOS không cho phép icon có vùng trong suốt (Apple sẽ tự động lấp nền
  # trắng khi build) — giữ true để plugin tự xử lý, tránh lỗi khi submit App Store.
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/icon/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package